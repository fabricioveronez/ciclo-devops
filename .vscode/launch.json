{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Debug DevContainer", 
            "skipFiles": [
                "<node_internals>/**"
            ],
            "cwd": "${workspaceFolder}/src",
            "program": "${workspaceFolder}/src/server.js"
        }
    ]
}