name: Install

on:
  workflow_dispatch:
      
jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - name: Print github context
        env:
          CONTEXT: ${{ github }}
        run: echo "$CONTEXT"



          
